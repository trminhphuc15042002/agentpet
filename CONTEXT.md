# Context

## Glossary

| Term | Definition |
|------|------------|
| **Usage total** | input + output + cache-read |
| **Cache** | OpenCode `tokens_cache_read` only |
| **Pet XP** | input + output; excludes cache-read |

`cache-write` and reasoning are out of scope for current Windows Phase 1.
